services:
  owncast:
    image: owncast/owncast:latest
    container_name: owncast
    restart: always
    ports:
      - "1935:1935"   # RTMP 推流端口
      - "8080:8080"   # 网页访问 + 管理后台
    volumes:
      - ./data:/app/data # 存放配置、聊天记录和视频缓存
